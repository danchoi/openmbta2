<% @alerts.each do |alert| %>
   <div class="alert <%= cycle('odd', 'even') %>">
    <h4><%= alert[:title] %>
      <span class="time"><%= format_time alert[:pubdate] %></span>
    </h4>
    <%= raw alert[:description] %>
  </div>

<% end %>
